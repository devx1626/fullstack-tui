node_modules/
dist/
.data/
.workspace/
*.log

# Authoring scratch: `node tools/audit.mjs` reports are local artifacts, not
# repo state (the CI gate is `npm run check`).
tools/audit-report.json
tools/audit-report.txt
